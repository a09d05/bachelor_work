<form method="post" action="{{ path('app_violations_rules_safe_delete', {'id': violations_rules_safe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ violations_rules_safe.id) }}">
    <button class="btn">Delete</button>
</form>
