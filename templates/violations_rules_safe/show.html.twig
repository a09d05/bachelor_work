{% extends 'base.html.twig' %}

{% block title %}ViolationsRulesSafe{% endblock %}

{% block body %}
    <h1>ViolationsRulesSafe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ violations_rules_safe.id }}</td>
            </tr>
            <tr>
                <th>Date_violation</th>
                <td>{{ violations_rules_safe.dateViolation ? violations_rules_safe.dateViolation|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ violations_rules_safe.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_violations_rules_safe_index') }}">back to list</a>

    <a href="{{ path('app_violations_rules_safe_edit', {'id': violations_rules_safe.id}) }}">edit</a>

    {{ include('violations_rules_safe/_delete_form.html.twig') }}
{% endblock %}
