{% extends 'base.html.twig' %}

{% block title %}FailedEmployees{% endblock %}

{% block body %}
    <h1>FailedEmployees</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ failed_employee.id }}</td>
            </tr>
            <tr>
                <th>DateFail</th>
                <td>{{ failed_employee.dateFail ? failed_employee.dateFail|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_failed_employees_index') }}">back to list</a>

    <a href="{{ path('app_failed_employees_edit', {'id': failed_employee.id}) }}">edit</a>

    {{ include('failed_employees/_delete_form.html.twig') }}
{% endblock %}
