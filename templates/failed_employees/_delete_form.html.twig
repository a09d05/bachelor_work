<form method="post" action="{{ path('app_failed_employees_delete', {'id': failed_employee.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ failed_employee.id) }}">
    <button class="btn">Delete</button>
</form>
