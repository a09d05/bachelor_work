{% extends 'base.html.twig' %}

{% block title %}CheckRequirementsWork{% endblock %}

{% block body %}
    <h1>CheckRequirementsWork</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ check_requirements_work.id }}</td>
            </tr>
            <tr>
                <th>PlannedCheckDate</th>
                <td>{{ check_requirements_work.plannedCheckDate ? check_requirements_work.plannedCheckDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>FactCheckDate</th>
                <td>{{ check_requirements_work.factCheckDate ? check_requirements_work.factCheckDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Result</th>
                <td>{{ check_requirements_work.result }}</td>
            </tr>
            <tr>
                <th>Comments</th>
                <td>{{ check_requirements_work.comments }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_check_requirements_work_index') }}">back to list</a>

    <a href="{{ path('app_check_requirements_work_edit', {'id': check_requirements_work.id}) }}">edit</a>

    {{ include('check_requirements_work/_delete_form.html.twig') }}
{% endblock %}
