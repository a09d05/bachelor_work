<form method="post" action="{{ path('app_check_requirements_work_delete', {'id': check_requirements_work.id}) }}" onsubmit="return confirm('Точно хотите удалить эту запись?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ check_requirements_work.id) }}">
    <button class="btn">Удалить</button>
</form>
