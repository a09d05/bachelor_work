{% extends 'base.html.twig' %}

{% block title %}PlannedTrainings{% endblock %}

{% block body %}
    <h1>PlannedTrainings</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ planned_training.id }}</td>
            </tr>
            <tr>
                <th>DateTrain</th>
                <td>{{ planned_training.dateTrain ? planned_training.dateTrain|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_planned_trainings_index') }}">back to list</a>

    <a href="{{ path('app_planned_trainings_edit', {'id': planned_training.id}) }}">edit</a>

    {{ include('planned_trainings/_delete_form.html.twig') }}
{% endblock %}
