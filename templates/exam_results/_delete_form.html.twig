<form method="post" action="{{ path('app_exam_results_delete', {'id': exam_result.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ exam_result.id) }}">
    <button class="btn">Delete</button>
</form>
