{% extends 'base.html.twig' %}

{% block title %}Редактирование{% endblock %}

{% block body %}
    <h1>Редактирование записи о сотруднике</h1>

    <tr>
        <td>{{ include('employee/_form.html.twig', {'button_label': 'Обновить'}) }}</td>
        <td>{{ include('employee/_delete_form.html.twig') }}</td>
    </tr>

    <a href="{{ path('app_employee_index') }}">Вернуться к списку</a>

{% endblock %}
